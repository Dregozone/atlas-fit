# Things that still need tweaks or to be implemented

- Show all available achievements with images and my best/max so far to see how close I am to hitting them
- Recommend the best food item based on my remaining available macros and whats known to the database, give a traffic light of red amber green like in BeeActive
- How are users meant to access the admin.manage-schedule page, ensure this works on a per-user basis so they only modify their own schedule
- Use flux chart to plot improvements/progress in weight, add a "Progress" page to see a few charts with benchmark exercises to show increase (potential) in 1RM weights
- Why are rest days on dashboard not showing the text "Rest day - Recovery is part of the program"?
- Include strava api listing off completed runs as well on a running/cardio page?

