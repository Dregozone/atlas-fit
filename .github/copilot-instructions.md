## Environment
- PHP 8.4 is required. Never assume PHP 8.3 or lower.
- Dependencies are managed with Composer.

## Testing
- All PRs must pass `php artisan test` before committing.
- Do not open a PR with failing tests.
- Run tests after making changes and fix any failures before finishing.

## Code Standards
- Follow PSR-12.
